import { FilterjobsPipe } from './filterjobs.pipe';

describe('FilterjobsPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterjobsPipe();
    expect(pipe).toBeTruthy();
  });
});
